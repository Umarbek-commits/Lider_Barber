{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo no-node-install",
  "buildCommand": "if [ -d flutter ]; then (cd flutter && git pull); else git clone https://github.com/flutter/flutter.git --depth 1 -b stable; fi && flutter/bin/flutter config --enable-web && flutter/bin/flutter build web --release --no-web-resources-cdn",
  "outputDirectory": "build/web"
}
